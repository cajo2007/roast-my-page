import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel-friendly defaults
  // TODO: Add image domains when adding OG image generation for share cards
};

export default nextConfig;
